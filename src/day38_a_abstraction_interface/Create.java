package day38_a_abstraction_interface;

public interface Create {
    public abstract void read();
    // In interface, if we have method as return type and the name
    // those, BY DEFAULT is "public abstract" method.


    void write();
    // In interface, if we have method as return type and the name
    // those, BY DEFAULT is "public abstract" method.

}
